name: face_net_authentication
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  tflite_flutter: ^0.8.0
  camera: ^0.8.1
  path_provider: ^2.0.1
  path: ^1.8.0
  firebase_ml_vision: ^0.12.0+1
  image: ^3.0.2
  firebase_core: ^1.0.4
  font_awesome_flutter: ^9.0.0
  url_launcher: ^6.0.3
  http: ^0.13.3
  mysql1: ^0.19.2
  geolocator: ^7.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/mobilefacenet.tflite
    - assets/logo.png
